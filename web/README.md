# Dependency Audit Studio UI

Local, fixture-backed frontend for the dependency auditor showcase. It does not
call a backend or modify dependency manifests.

```bash
npm install
npm run dev
```

Validation:

```bash
npm run typecheck
npm run lint
npm run test
npm run build
```
